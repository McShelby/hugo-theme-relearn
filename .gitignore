.claude/settings*
# CI checks hugo-theme-relearn-infra out here, inside this working tree, so
# that workflows can use its composite actions. It must never be committed.
/infra/
.hvm
.DS_Store
.githooks/hooks.log
.hugo_build.lock
docs/public*
exampleSite/public*
hugo*.exe
*.xcf
*.zip
